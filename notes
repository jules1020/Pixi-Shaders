NOTES

Shaders to edit:
jnoise
	-- need to animate
outline
	-- need to figure out why everything turns white
passthrough
	-- no effect
shaker
	-- some effect, not sure if correct
slitscan
	-- black screen, no effect
swell
	-- black screen, no effect
twist
	-- black screen, no effect
bleachbypass
	-- not building correctly
blend
    -- no effect, may need animation
    -- there are 2 frags and 2 vertexes
chroma
	-- no effect, may need to edit the frag
displacement
	-- filter registering as 'null' in console
tone
	-- no longer has a defined value?? check the git you pulled from

***************************** TRY CHANGING 'defaultValue' UNIFORM TO 'value' **************************************
lumakey
	-- no effect, nothing happened when animated
nightvision
	-- need to fix frag; partially dependent on original js
opticalflow
	-- black screen, no effect, nothing happened with animation, not catching errors
panorama
	-- frag dependent on function from original js
pixelate
	-- black screen, no effect
******************************************************************************************************************
sepia
	-- something wrong with the way the matrix is declared
temperature
	-- black screen, no effect
tvglitch
	-- need to work on the fragment, dependent on original js
ascii
	-- fix fragment location, may need to be updated?

PACKAGES FOR SUBLIME
ESLint
Javascript Beautify
Package Control
SublimeLinter
SublimeLinter-contrib-eslint
SublimeLinter-jscs

GET FUCKING SOURCETREE

REFERENCE FOR HOW TO BUILD JSON http://browsenpm.org/package.json#main
npm install --dev

